services:
  - type: web
    name: my-app
    buildCommand: chmod +x ./gradlew && ./gradlew bootRun
    startCommand: ./gradlew bootRun
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
    healthCheckPath: /
    runtime: node
    port: 8080