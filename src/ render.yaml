services:
  - type: web
    name: my-app
    buildCommand: ./gradlew bootRun
    startCommand: ./gradlew bootRun
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
    healthCheckPath: /
    runtime: node
    port: 10000